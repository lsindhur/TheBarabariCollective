// ✅ DOM Properties

// Once you've selected an element, you can use **DOM properties**
// to read or change its content and structure.

// some important DOM properties are :

// 📌 innerText
// Gets or sets only the visible text of the element (ignores hidden elements)

//change the text content inside ul to "I am setting something"

// 📌 innerHTML
// Gets or sets the full HTML content inside the element (can include tags)



// 📌 textContent
// Gets or sets all text content inside the element, including hidden text




