//while object methods are Functions defined inside an object which act on that
//specific object

//object utility methods are Built-in methods which work on all objects


//Object.keys(obj)
//Returns an array of keys in the object.

const user = {
    name: "Alice",
    age: 30,
    city: "New York"
  };
  //console.log(Object.keys(user));


//Object.values(obj) //??
/* console.log(Object.values(user)); */


//Object.entries(obj) //?? 
console.log(Object.entries(user));

