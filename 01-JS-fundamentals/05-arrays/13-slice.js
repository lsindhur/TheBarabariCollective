const planets = ["Earth","Mars","Jupiter","Saturn"];


//slice
 //slice methods returns a selected range of array elements 
 //does not modify the original array

 //synatx
 //slice(start,end) 

//range //the start is always inclusive //but end is always exclusive

 //example
const newArr = (planets.slice(0,2));
console.log(newArr);
console.log(planets);

